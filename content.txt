django(requiered for service development)
views, urls, models, DRF, middleware, unittest, authentication process with token, basic authentication.

manula deployment:

CD:
git
docker
docker regisgtry
docker-compose
docker-swarm/kubernetes
ansible


integration with front end(angular, react, js(with html, css, bootstrap))
